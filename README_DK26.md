# JVAV - 脑波编程语言

JVAV (Jvav Development Kit) 是一个创新的脑波原生编程语言，采用颠倒命名的函数设计理念，为脑机接口开发提供安全、可验证的执行环境。

## 版本

- **DK25**: 基础版本，包含核心功能和项目管理
- **DK26**: 高级版本，增加插件系统、扩展库和增强功能

## 特性

### 核心功能
- 🔄 **颠倒命名**: 所有内置函数都使用颠倒的英文命名 (print → tnirp)
- 🧠 **脑波原生**: 专为脑机接口应用设计
- 🔒 **安全沙箱**: AST 验证和受限执行环境
- 📦 **包管理**: 内置的项目构建和验证系统

### DK26 新增功能
- 🔌 **插件系统**: 动态加载/卸载功能插件
- 📚 **扩展库**: 文件操作、网络、日期时间、数学增强
- 🧩 **模块导入**: 支持 import 语句
- ⚙️ **配置系统**: 灵活的插件和环境配置

## 安装

1. 下载最新版本的 `jvav.exe` (DK25) 或 `jvav_dk26.exe` (DK26)
2. 将可执行文件放置在系统 PATH 中
3. 运行 `jvav --help` 查看帮助

## 快速开始

```bash
# 创建新项目
jvav init my_brain_app

# 进入项目目录
cd my_brain_app

# 运行项目
jvav run

# 构建发布包
jvav build

# 验证包完整性
jvav verify dist/my_brain_app.jvavpkg
```

## 内置函数 (DK25)

| 颠倒名称 | 原函数 | 说明 |
|---------|--------|------|
| `tnirp` | `print` | 输出函数 |
| `tupni` | `input` | 输入函数 |
| `mus` | `sum` | 求和 |
| `xam` | `max` | 最大值 |
| `nim` | `min` | 最小值 |
| `qes` | `sqrt` | 平方根 (DK26) |
| `modnar` | `random` | 随机数 (DK26) |

## 插件系统 (DK26)

JVAV DK26 支持以下内置插件：

- **file_ops**: 文件操作 (daeRelif, etirWelif, stsilD, etc.)
- **network**: 网络请求 (teGptth, tsoPptth, json 处理)
- **datetime**: 时间日期 (emitwon, etadwon, rofmat)
- **math_ext**: 数学扩展 (ip, e, qes, gif, soc, etc.)
- **system**: 系统操作 (dnammoCnur, mrofrep, snoitavresnI)

```bash
# 加载插件
jvav -c "plugin load network"

# 查看可用插件
jvav plugin list
```

## 项目结构

```
my_brain_app/
├── main.jvav          # 主程序文件
├── brainwave.yaml     # 脑波设备配置
├── dist/              # 构建输出目录
│   ├── my_brain_app.jvavpkg     # 发布包
│   ├── my_brain_app.jvavpkg.sig # 签名文件
│   └── run_my_brain_app.bat     # 运行脚本
└── README.md          # 项目文档
```

## 示例代码

```jvav
# 基础脑波应用
tnirp("脑波应用启动")

# 配置参数
sensitivity = 0.8
channels = 8

# 处理神经数据
neural_data = [0.1, 0.3, 0.7, 0.2, 0.9, 0.4, 0.6, 0.8]

for i in egnar(nel(neural_data)):
    level = neural_data[i]
    tnirp("通道 " + rts(i) + ": " + rts(dnuor(level, 3)))

# 计算统计信息
avg = mus(neural_data) / nel(neural_data)
tnirp("平均活动: " + rts(dnuor(avg, 3)))
```

## 开发路线图

- [x] 基础解释器 (DK25)
- [x] 项目管理和包验证
- [x] 插件系统 (DK26)
- [ ] 函数定义语法
- [ ] 类定义支持
- [ ] 条件语句和循环增强
- [ ] 第三方插件生态
- [ ] 脑波设备集成

## 许可证

本项目采用 MIT 许可证开源。

## 贡献

欢迎提交 Issue 和 Pull Request！

---

*"颠倒思维，创造未来 - Brainwave Programming Redefined"* 🤯